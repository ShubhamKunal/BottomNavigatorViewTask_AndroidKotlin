<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/profile_nav"
    app:startDestination="@id/profileFragment">

    <fragment
    android:id="@+id/profileFragment"
    android:name="com.example.task2.profileFragment"
    android:label="Profile"
    tools:layout="@layout/fragment_profile">
        <action android:id="@+id/PtoP"
            app:destination="@+id/profile_opened"/>
    </fragment>
    <fragment
        android:id="@+id/profile_opened"
        android:name="com.example.task2.profile_opened"
        android:label="Profile"
        tools:layout="@layout/fragment_profile_opened" />

</navigation>