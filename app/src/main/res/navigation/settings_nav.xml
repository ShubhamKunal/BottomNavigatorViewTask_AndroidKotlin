<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/settings_nav"
    app:startDestination="@id/settingsFragment">

    <fragment
        android:id="@+id/settingsFragment"
        android:name="com.example.task2.settingsFragment"
        android:label="Settings"
        tools:layout="@layout/fragment_settings">
        <action android:id="@+id/StoS"
            app:destination="@+id/settings_opened"/>
    </fragment>

    <fragment
        android:id="@+id/settings_opened"
        android:name="com.example.task2.settings_opened"
        android:label="Settings"
        tools:layout="@layout/fragment_settings_opened" />

</navigation>